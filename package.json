{
  "name": "restoration-zonal-diff",
  "version": "0.1.0",
  "private": true,
  "description": "Layout-audit tooling for restoration-zonal-diff. The site itself is plain HTML/CSS/JS or mkdocs.",
  "type": "module",
  "scripts": {
    "audit:layout": "node tools/audit-layout.mjs",
    "audit:layout:ci": "node tools/audit-layout.mjs --json-only",
    "audit:install": "npx playwright install --with-deps chromium"
  },
  "devDependencies": {
    "playwright": "^1.47.0"
  },
  "engines": {
    "node": ">=18"
  }
}
